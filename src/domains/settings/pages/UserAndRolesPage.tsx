import React from 'react';
import { UnderConstruction } from '@/components';

export const UserAndRolesPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de usuarios y roles"
      message="El submodulo de usuarios y roles está en construcción. Por favor, regresa pronto."
    />
  );
};

export default UserAndRolesPage;
