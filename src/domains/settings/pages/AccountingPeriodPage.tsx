import React from 'react';
import { UnderConstruction } from '@/components';

export const AccountingPeriodPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de periodo contable"
      message="El submodulo de periodo contable está en construcción. Por favor, regresa pronto."
    />
  );
};

export default AccountingPeriodPage;
