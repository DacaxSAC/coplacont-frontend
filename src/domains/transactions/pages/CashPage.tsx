import React from 'react';
import { UnderConstruction } from '@/components';

export const CashPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de caja"
      message="El submodulo de caja está en construcción. Por favor, regresa pronto."
    />
  );
};