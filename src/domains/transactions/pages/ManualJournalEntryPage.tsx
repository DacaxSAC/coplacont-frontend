import React from 'react';
import { UnderConstruction } from '@/components';

export const ManualJournalEntry: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de asiento contable manual"
      message="El submodulo de asiento contable manual está en construcción. Por favor, regresa pronto."
    />
  );
};

export default ManualJournalEntry;
