import React from 'react';
import { UnderConstruction } from '@/components';

export const MainPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de compras"
      message="El submodulo de compras está en construcción. Por favor, regresa pronto."
    />
  );
};