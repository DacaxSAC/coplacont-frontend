import React from 'react';
import { UnderConstruction } from '@/components';

export const InventoryAdjustment: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de ajuste de inventario"
      message="El submodulo de ajuste de inventario está en construcción. Por favor, regresa pronto."
    />
  );
};

export default InventoryAdjustment;
