import React from 'react';
import { UnderConstruction } from '@/components';

export const KardexPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de kardex"
      message="El submodulo de kardex está en construcción. Por favor, regresa pronto."
    />
  );
};
