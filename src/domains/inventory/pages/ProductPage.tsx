import React from 'react';
import { UnderConstruction } from '@/components';

export const ProductPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de productos"
      message="El submodulo de productos está en construcción. Por favor, regresa pronto."
    />
  );
};
