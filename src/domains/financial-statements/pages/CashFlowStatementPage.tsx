import React from 'react';
import { UnderConstruction } from '@/components';

export const CashFlowStatementPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de flujo de efectivo"
      message="El submodulo de flujo de efectivo está en construcción. Por favor, regresa pronto."
    />
  );
};

export default CashFlowStatementPage;
