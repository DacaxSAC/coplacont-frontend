import React from 'react';
import { UnderConstruction } from '@/components';

export const IncomeStatementPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de estado de resultados"
      message="El submodulo de estado de resultados está en construcción. Por favor, regresa pronto."
    />
  );
};

export default IncomeStatementPage;
