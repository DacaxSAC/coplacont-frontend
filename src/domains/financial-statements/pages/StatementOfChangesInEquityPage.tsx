import React from 'react';
import { UnderConstruction } from '@/components';

export const StatementOfChangesInEquityPage: React.FC = () => {
  return (
    <UnderConstruction
      title="Submodulo de estado de patrimonio"
      message="El submodulo de estado de patrimonio está en construcción. Por favor, regresa pronto."
    />
  );
};

export default StatementOfChangesInEquityPage;
